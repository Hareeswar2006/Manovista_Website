    <%- include('./partials/header.ejs') %>
    <title>Manovista Consulting</title>
    <link rel="stylesheet" href="/css/home.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <section class="hero-section">
        <div class="hero-bg-image"></div>
        <div class="overlay"></div>
        <div class="hero-content fade-in">
            <h1>Strategic Financial Insights. Smarter Decisions. Sustainable Growth.</h1>
            <p>At Manovista Consulting LLP, we handle your business registration, compliance, and financial planning—so you can focus on scaling your vision with confidence.</p>
            <ul class="service-highlights">
                <li><i class="fas fa-check-circle"></i> Accounting and Bookkeeping Services</li>
                <li><i class="fas fa-file-invoice-dollar"></i> Tax Preparation and filings</li>
                <li><i class="fas fa-chart-line"></i> Tax Compliance and Advisory</li>
                <li><i class="fas fa-clipboard-check"></i> Internal & External Audit Support</li>
                <li><i class="fas fa-balance-scale"></i> Preparation of Financial Statements</li>
            </ul>
        </div>
    </section>

    <section class="about-us-section content fade-section">
        <div class="about-left slide-in-left">
            <img src="https://assets.zyrosite.com/cdn-cgi/image/format=auto,w=328,h=376,fit=crop/m2W8yD1GLRSbX9Mr/1-3-YD0lKoL3QVCvQvk0.jpg" alt="Manovista Team">
            <div class="quote">
                <p><strong>❝ Expert guidance for your business needs.</strong></p>
                <p><strong>— Manovista</strong></p>
            </div>
        </div>
        <div class="about-right slide-in-right">
            <h2>About Us</h2>
            <h3>Manovista Consulting LLP: Your Trusted Partner for Business Consulting & Financial Empowerment.</h3>
            <p>
                At Manovista Consulting LLP, we provide expert business consulting solutions specializing in
                Accounting, CFO services, and HR management for companies worldwide.
                Our mission is to deliver reliable, cost-effective support that helps businesses streamline operations,
                improve decision-making, and focus on sustainable growth.
            </p>
            <p>
                At Manovista Consulting LLP, we believe in trust, integrity, and innovation,
                ensuring that every business regardless of size has access to world-class financial and HR solutions.
                Let us be your trusted partner in achieving sustainable business excellence.
            </p>
            <a href="/about" class="btn btn-secondary about-btn">More About Us</a>
        </div>
    </section>

    <section class="services-section content fade-section">
        <h2>Our Services</h2>
        <div class="services-grid">
            <div class="service-card slide-in-up">
                <h3>Accounting and Bookkeeping Services</h3>
                <ul>
                    <li><i class="fas fa-book"></i> Bookkeeping Services</li>
                    <li><i class="fas fa-file-invoice-dollar"></i> Tax Preparation and filings</li>
                    <li><i class="fas fa-chart-line"></i> Tax Compliance and Advisory</li>
                    <li><i class="fas fa-clipboard-check"></i> Internal & External Audit Support</li>
                    <li><i class="fas fa-balance-scale"></i> Preparation of Financial Statements</li>
                </ul>
            </div>

            <div class="service-card slide-in-up">
                <h3>Management Consulting Services</h3>
                <ul>
                    <li><i class="fas fa-user-tie"></i> Virtual CFO Services</li>
                    <li><i class="fas fa-coins"></i> Financial Consulting</li>
                    <li><i class="fas fa-sync-alt"></i> Business Process Optimization</li>
                    <li><i class="fas fa-rocket"></i> Startup advisory and Growth Strategies</li>
                    <li><i class="fas fa-shield-alt"></i> Risk Management and Business Planning</li>
                </ul>
            </div>

            <div class="service-card slide-in-up">
                <h3>HR Provision & Management</h3>
                <ul>
                    <li><i class="fas fa-users"></i> HR Staffing and Recruitment Services</li>
                    <li><i class="fas fa-handshake"></i> Manpower Supply and Outsourcing</li>
                    <li><i class="fas fa-money-check-alt"></i> Payroll and Compliance Support</li>
                    <li><i class="fas fa-cogs"></i> HR Operations Management</li>
                    <li><i class="fas fa-chalkboard-teacher"></i> Employee Training Coordination</li>
                </ul>
            </div>
            <div class="service-card slide-in-up">
            <h3>Incorporation Services</h3>
            <ul>
                <li><i class="fas fa-building"></i> Private Limited Company</li>
                <li><i class="fas fa-book"></i> Limited Liability Partnership</li>
                <li><i class="fas fa-user"></i> One Person Company (OPC)</li>
                <li><i class="fas fa-money-check-alt"></i> Sole Proprietorship</li>
                <li><i class="fas fa-handshake"></i> Partnership</li>
            </ul>
        </div>
        </div>
    </section>

    <section class="why-manovista content fade-section">
        <h2>Why Choose Manovista?</h2>
        <div class="stats-cards">
            <div class="card fade-in-up">
                <h3 class="counter" data-target="35">0</h3><p class="counter-suffix"></p>
                <p>Startups Served<br>Across India</p>
            </div>
            <div class="card fade-in-up delay-1">
                <h3 class="counter" data-target="50">0</h3><p class="counter-suffix"></p>
                <p>Company Filings<br>Every Month</p>
            </div>
            <div class="card fade-in-up delay-2">
                <h3 class="counter" data-target="100">0</h3><p class="counter-suffix"></p>
                <p>MSMEs<br>Empowered</p>
            </div>
            <div class="card fade-in-up delay-3">
                <h3 class="counter" data-target="150">0</h3><p class="counter-suffix"></p>
                <p>Bookkeeping &<br>Accounting Services</p>
            </div>
        </div>
    </section>
<%- include('./partials/footer.ejs') %>
    <script>
        // --- Intersection Observer for Fade-in/Slide-in Animations ---
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                    // Special handling for counter animation
                    if (entry.target.classList.contains('why-manovista')) {
                        startCounters();
                    }
                } else {
                    // Optional: remove 'visible' class if you want elements to disappear when scrolling up
                    // entry.target.classList.remove('visible');
                    // Reset counters if they go out of view (optional, can be removed)
                    // if (entry.target.classList.contains('why-manovista')) {
                    //     resetCounters();
                    // }
                }
            });
        }, {
            threshold: 0.1 // Adjust as needed
        });

        document.querySelectorAll('.fade-section, .slide-in-left, .slide-in-right, .slide-in-up, .fade-in-up, .hero-content.fade-in').forEach(element => {
            observer.observe(element);
        });

        // --- Counter Animation Script ---
        const counters = document.querySelectorAll('.counter');
        const speed = 1500; // Increased speed for slower, more noticeable count (was 200)

        let countersStarted = false; // Flag to ensure counters only start once per page load

        function startCounters() {
            if (countersStarted) return; // Prevent multiple starts
            countersStarted = true;

            counters.forEach(counter => {
                const target = +counter.getAttribute('data-target');
                const suffixElement = counter.nextElementSibling;
                const suffix = suffixElement && suffixElement.classList.contains('counter-suffix') ? suffixElement.innerText : '';
                let count = +counter.innerText.replace(suffix, ''); // Remove existing suffix for calculation

                // Calculate increment based on desired duration (e.g., 2 seconds for animation)
                const duration = 6000; // 2 seconds
                const steps = duration / 30; // Update every 10ms for smooth animation
                const increment = target / steps;

                const updateCount = () => {
                    if (count < target) {
                        count = Math.min(target, count + increment); // Ensure we don't exceed target
                        counter.innerText = Math.ceil(count); // Update number only
                        if (suffixElement) suffixElement.style.opacity = '1'; // Ensure suffix is visible
                        requestAnimationFrame(updateCount); // Use requestAnimationFrame for smoother animation
                    } else {
                        counter.innerText = target+"+";
                        if (suffixElement) suffixElement.style.opacity = '1'; // Ensure suffix is visible
                    }
                };
                updateCount(target,30);
            });
        }

        // Optional: Function to reset counters (if you uncomment the else block in observer)
        // function resetCounters() {
        //     counters.forEach(counter => {
        //         counter.innerText = '0';
        //         const suffixElement = counter.nextElementSibling;
        //         if (suffixElement && suffixElement.classList.contains('counter-suffix')) {
        //             suffixElement.style.opacity = '0'; // Hide suffix on reset
        //         }
        //     });
        //     countersStarted = false;
        // }

    </script>
        